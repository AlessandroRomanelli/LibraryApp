table
  thead
    tr
      th Title
      th Author
      th Genre
      th Year Released
  tbody
    each loan in loans
      - var book = loan.book;
      tr
        td
          a(href=`books/edit/${book.id}`)=book.title
        td=book.author
        td=book.genre
        td=book.first_published
